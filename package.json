{
  "name": "aidran-platform-scaffold",
  "version": "0.0.0",
  "private": true,
  "description": "AIDRAN platform scaffold — open-source reference implementation for the discourse-intelligence pipeline (Apache 2.0)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r --parallel run build",
    "typecheck": "pnpm -r --parallel run typecheck",
    "test": "pnpm -r --parallel run test"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  },
  "license": "Apache-2.0"
}
